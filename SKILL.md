---
name: rabbitQ-skill-lark-xhs
description: 小兔Q彬 · 将飞书云文档导出的 Markdown、图片附件或完整 ZIP 包，独立解析为可编辑的小红书 3:4 图文 Studio。支持全封面/半封面/无封面三种形式、AI 封面生成后直插、手动分页（---）、连续分页、主题组合、引用/卡片/序列/表格/macOS 代码块、图片裁剪与并排、草稿保存、一键复原及 PNG ZIP 批量导出。
version: 0.9.20
metadata:
  author: 小兔Q彬 / rabbitQ
  category: xiaohongshu
  platform: xiaohongshu
  input: lark-markdown-package
  requires:
    runtime: node
    npm: ["cheerio", "html2canvas", "jszip"]
  output: "{slug}-xhs/xhs-studio.html"
---

# rabbitQ-skill-lark-xhs · 飞书 Markdown 转小红书图文

将飞书云文档导出的 Markdown 与图片附件，直接生成本地可编辑的 `xhs-studio.html`，并批量导出标准 3:4 PNG。

## 定位与边界

- 输入是飞书导出的 Markdown 文件、包含 Markdown 与附件的目录，或完整 ZIP。
- 输出是本地 Studio 和 manifest，不会自动发布到小红书。
- 小红书图文只放图片。视频链接和视频附件必须移除，并向用户明确提示“视频未放入图文，需要另行上传或先截帧”。

## 触发条件

- 用户调用 `/rabbitQ-skill-lark-xhs`、`/rabbitq-xhs`、`/xhs-studio`。
- 用户提供飞书 Markdown、导出目录或 ZIP，要求生成小红书 3:4 图文、编辑 HTML 或 PNG 图组。
- 用户要求重新运行、修复或验证由本 Skill 生成的 `xhs-studio.html`。

## 输入契约

接受以下任一输入：

1. 单个 `.md` 文件。
2. 顶层包含一个 `.md` 文件和附件目录的飞书导出包。
3. 包含上述结构的 `.zip`（自动解压，无需用户手动解压）。
4. **飞书云文档链接**（`/wiki/` 或 `/docx/` URL）——由 AI 先导出成 Markdown 包（见下节），再走主流程。

典型目录：

```text
文章目录/
├── 文章.md
└── 图片和附件/
    ├── image.png
    └── image 1.png
```

规则：

- 目录输入优先读取顶层 Markdown；ZIP 会解压到临时目录后递归定位 Markdown。
- 图片路径可包含中文、空格和 URL 编码字符。
- 标题优先级：CLI `--title` > frontmatter `title` > **文首中文标签**（`标题：` / `大标题：`）> 第一个 H1 > 文件名。
- 副标题优先级：CLI `--subtitle` > frontmatter `subtitle` > **文首中文标签**（`副标题：`）> **AI 根据云文档总结**（见下）> Studio 可编辑占位符。
- **用户在 Markdown 里指定封面时**，`convert.js` 会自动解析，无需 AI 改写：
  - YAML frontmatter：`title:` / `subtitle:`
  - 文首中文行：`标题：…`、`大标题：…`、`副标题：…`（也支持 `封面标题：` / `封面副标题：`）
- AI 的职责：发现已有字段就尊重，**仅缺副标题**时才读全文补一句。
- 所有输出画布必须接近 3:4，否则转换器应报错而不是静默拉伸。

## 飞书链接输入：AI 导出流程

用户直接给飞书云文档链接时，AI 自己导出 Markdown 包，不需要用户手动导出：

1. **环境检查**：运行 `lark-cli --version`。命令不存在时引导用户安装并登录：
   ```bash
   npm install -g @larksuite/cli
   lark-cli auth login --scope "wiki:wiki:readonly docx:document:readonly"
   ```
   把授权链接发给用户，等用户在浏览器里完成后继续。
2. **导出**：在 Skill 根目录运行：
   ```bash
   node scripts/lark-export.js <飞书URL> -o <输出目录>
   ```
   脚本自动完成：`docs +fetch` 取文档 → 清洗飞书私有标签（image/callout/grid/lark-table 等）→ `docs +media-download` 逐张下载图片到 `assets/` → 产出 `<输出目录>/<标题>.md + assets/ + _image_tokens.json`。
   - 已验证环境：`lark-cli 1.0.65`。新旧版本 `+fetch` 返回格式不同（`document.content` XML / `markdown`），脚本内部已兼容，AI 不需要分别处理。
   - **飞书原生样式会随导出带过来**：文档里的加粗 → `**`、下划线 → `++`、高亮块（callout）→ `>` 引用，转换时直接生效，不要在第二遍样式判断里重复标记同一段文字。
3. **授权失败处理**：脚本以退出码 2 结束时，把下面这句发给用户并等其完成授权后重跑同一条命令：
   `lark-cli auth login --scope "wiki:wiki:readonly docx:document:readonly"`
   文档对当前账号不可见（403/权限不足）时，如实告知用户去文档分享设置里开权限，不要反复重试。
4. 导出成功后，把输出目录当作标准输入包，回到主流程第 3 步继续（通读全文、两遍走样式判断、确认封面形式后转换）。

规则：

- 导出速度受图片数量影响，提前告知用户"图多的文档会慢一些"，不要静默卡在下载阶段。
- 附件和视频不下载，Markdown 里以 `**[附件: 名称]**` 标注；画板保留 `*(画板: token)*` 占位。
- 重跑同一文档时已下载的图片自动跳过。

## 执行流程

1. 完整读取本 `SKILL.md`。
2. 确认输入路径存在，并判断是 Markdown、目录还是 ZIP。
3. **通读源 Markdown 全文**（不是只看标题）。检查封面字段是否已指定：
   - YAML frontmatter（`---` 块里的 `title:` / `subtitle:`）
   - 文首中文标签（`标题：` / `大标题：` / `副标题：`）
   - CLI `--title` / `--subtitle`
   - **已有任一来源** → 直接转换，不要用 AI 覆盖。
   - **缺 `subtitle` 且用户未指定** → AI 读完全文后总结，通过 `--subtitle` 传入。
   - **样式两遍走（每次转换必做）**：第一遍按 Markdown 符号照实转换；第二遍由 AI 通读全文做样式判断，产出调整后的中间稿再转换。
     - 第二遍·行内强调（任何原稿都要做）：通读全文后自行判断哪些词句值得强调——关键结论、警示、金句、操作路径、核心数字。有色字用 `==文字==`，下划线用 `++文字++`，加粗用 `**文字**`。用不用、用几个完全由 AI 看着来：合适才标，宁缺毋滥，绝不允许一句不标交差，也不许满篇都是标记。
     - 第二遍·结构推断（原稿除大标题外几乎没有结构标记时必做）：找章节（"先说 / 第一步 / 接下来 / 最后"式语义 → 拟短标题提为一级标题）、找小节（并列小点 → 二级标题或列表）、找罗列（清单式内容 → 无序/有序列表）、找块级强调（结论/提醒/金句 → 卡片，引语 → 引用块）。
     - 批判性自检：不能把每段都提成标题（一章一个）；步骤序列不能漏编号；只加标记，不改原文事实与措辞；飞书转义符（`\~`、`\+`、`\.` 等）由转换器按 CommonMark 规则还原，中间稿里保留原样即可。
     - **兜底**：即使原稿一个 Markdown 标记都没有，也必须完成第二遍判断后再转换，不允许整篇按纯段落输出。一级/二级标题、加粗、卡片、有色字、下划线、引用都是候选项，绝不要求每种样式都安排上。
     - 第二遍改动较多时先把提纲给用户确认；改动很少（≤3 处）可直接转换，并在结果里说明做了哪些样式调整。
4. 主动让用户选择封面形式：**全封面 / 半封面 / 无封面**。没有明确选择时不要擅自生成图片；可简短说明差异后等待选择：
   - **全封面**：首张为独立的完整 3:4 图片，生成尺寸固定为 **1080×1440**，正文从第 2 页开始。生成前必须另外确认最终标题与副标题；二者会直接进入图片，确认后才可调用图像生成。用 `--cover-mode full`。
   - **半封面**：上半页配图，图片生成尺寸固定为 **1080×720（3:2）**；下半页为 Studio 内可编辑的标题与副标题。未指定构图时，默认采用“**左侧短标题/钩子 + 右侧 IP 或主题主视觉**”的横幅构图；图片内最多一个两行钩子（中文建议不超过 8 字，英文建议不超过 4 个词），只负责第一眼点题，禁止照抄或压缩复述下半页完整标题。用 `--cover-mode half`。
   - **无封面**：不生成封面图，上半页为可编辑标题与副标题，下半页直接接续正文。用 `--cover-mode none`。
   - **默认生成策略**：用户只确定封面形式、没有给画风时，按文章主题生成干净、可读的主视觉；全封面默认使用“主题配图 + 已确认标题/副标题”的图文一体构图；半封面默认使用“左短字、右主角/核心视觉”，下半页继续承载可编辑的完整标题与副标题。若没有 IP，则右侧改用文章主题对应的物体、人物或场景，不留成纯装饰空图。
   - **IP 与 Logo 的使用**：用户明确希望封面出现 IP 时，可询问“是否提供 IP 参考图”；未提供时按文章主题生成，不假定有固定角色。飞书、小红书等来源/去向 Logo 只用于明确的转换流程、工具介绍或用户点名要求的封面，不作为普通文章的默认装饰。
   - **生图能力自检**：用户选择全封面或半封面并需要 AI 配图时，先确认当前会话有可调用的图像生成能力；可用时生成 PNG/JPG 并通过 `--cover-image <绝对路径>` 一并传给转换器。不可用时必须如实说明，并给出替代方案：让用户提供本地封面图、使用现有素材，或先生成半封面/无封面的可编辑 HTML；不得假称已生成图片。
5. 在 Skill 根目录运行 `node scripts/ensure-font.js`：先检查 `Noto Serif SC`；macOS 缺失且已安装 Homebrew 时自动执行 `brew install --cask font-noto-serif-sc`。安装后重新打开浏览器，确保新字体生效。不得用 Apple 私有 `STSongti-*` 映射替代这一步。
6. 若 `node_modules` 不存在，在 Skill 根目录运行 `npm ci`。
7. 调用 `scripts/convert.js` 生成 Studio：始终显式传入用户选择的 `--cover-mode`；仅缺副标题时带 `--subtitle`；有 AI 封面时同时带 `--cover-image`。
8. 返回可点击的 `xhs-studio.html` 绝对路径，并告知用户采用的副标题文案。
9. 若有视频警告，必须在结果中说明视频已跳过。
10. 对真实页面进行视觉验证；不能只报告脚本退出码。默认停在 Studio 预览，不导出 PNG ZIP。
11. 只有用户明确要求“导出”“验收”“准备发布/发小红书”或“核对 PNG”时，才实际导出 PNG ZIP。

### AI 副标题生成（缺省时必做）

`convert.js` **不会**从正文自动猜副标题。但 **会**自动读 frontmatter 和文首中文标签里的封面字段——**不靠 AI 猜**。

**何时需要 AI：**

- frontmatter / 中文标签 / CLI **都没有** `subtitle`，且用户也没口头指定 → **AI 读完全文后生成**，写入 `--subtitle`。
- 任一来源 **已有** `title` / `subtitle` → 直接交给 `convert.js`，AI **不要覆盖**。

**用户在 Markdown 里直接指定封面（两种写法都行）：**

YAML frontmatter：

```yaml
---
title: 飞书云文档转3:4图文skill开源啦
subtitle: 飞书 Markdown 直转可编辑 3:4 图文
---
```

文首中文标签（紧挨正文开头，中间可空行）：

```markdown
标题：飞书云文档转3:4图文skill开源啦
副标题：飞书 Markdown 直转可编辑 3:4 图文，写完就能批量出 PNG

# 正文从这里开始
```

也认 `大标题：`、`封面标题：`、`封面副标题：`。识别后会从正文删掉这两行，只留在封面。

**怎么写：**

- 一句话钩子，补充主标题、不重复主标题。
- 说清「这篇讲什么 / 读者得什么」，偏结论或场景，不要目录式罗列。
- 语气贴近小红书封面：直接、有信息量，避免「本文介绍了…」。
- 长度：按 Studio 限制，**最多约 48 个中文字符权重、最多两行**；英文/数字按较短侧估算，宁可短一点。
- 不要用 Markdown 符号（`#`、`**`、链接）；不要带句号结尾也行，看语感。

**参考句式：**

- 「这次不是讲概念，是一次真实踩坑复盘。」
- 「两件真实 skill 开发复盘，教你什么工作该交给 AI。」
- 「从飞书 Markdown 到 3:4 图文，排版终于能自己控了。」

**命令示例：**

```bash
node "scripts/convert.js" "article.md" \
  --subtitle "两件真实 skill 开发复盘，教你什么工作该交给 AI" \
  --cover-mode full \
  --cover-image "/path/to/cover.png"
```

基本命令：

```bash
node "scripts/convert.js" article.md
node "scripts/convert.js" "/path/to/lark-export-package"
node "scripts/convert.js" article.md -o "/path/to/output-xhs"
```

完整参数：

```text
-o, --output-dir <dir>   输出目录，默认在源 Markdown 旁生成 <slug>-xhs
--title <text>           覆盖标题
--subtitle <text>        指定封面副标题
--cover-image <file>     本地 PNG/JPG/WebP 封面图，转换时自动嵌入首张封面
--cover-mode <mode>      封面形式：full（全封面）/ half（半封面）/ none（无封面），默认 half
--keywords <a,b,c>       兼容保留的关键词元数据
--size <WxH>             3:4 画布，默认 1080x1440
--width <px>             画布宽度
--height <px>            画布高度
--help                   查看帮助
```

## Markdown 到 Studio 的映射

**三层分离（封面 vs 正文，不要混用）：**

| 层级 | 识别来源 | 是否进正文 |
|---|---|---|
| **封面大标题** | `frontmatter title` > CLI `--title` > 文首 `标题：` / `大标题：` > 首个 `#`（仅当未显式指定时）> 文件名 | 否 |
| **封面副标题** | `frontmatter subtitle` > CLI `--subtitle` > 文首 `副标题：` > AI 总结 | 否 |
| **正文标题** | 文内所有 `#` / `##` / …，按实际深度相对映射 | 是 |

**正文标题规则：**

- 仅当某行标题**文案与已解析的封面大标题相同**时，才从正文跳过（避免封面与正文重复）。
- **不会**一律跳过文档里所有 `#`；有些稿确实用 `#` 做正文一级章节。
- 典型飞书导出（无 frontmatter）：首个 `#` 同时充当封面大标题，同文标题不进正文；`## 01 …` 映射为正文一级。
- 若你已用 frontmatter / CLI / 中文标签指定封面，且正文**同时有** `#` 与 `##`：单个 `#` 视为导语（加粗，不编号），`## 01 …` 才是正文一级。
- 若全文只用 `#` 做章节（没有 `##`），`#` 仍正常映射为正文一级。

**正文深度映射（在排除封面重复标题之后）：**

1. 文内**最浅**的 `#` 深度 → 正文一级（常见 `## 01 …` → 大号 01/02 + 通栏线）
2. **第二浅**的 `#` 深度 → 正文二级（常见 `##### 小节` → 强调色 + 下划线）
3. 更深的 `#` → 加粗正文段落

| 输入结构 | 输出结构 |
|---|---|
| frontmatter `title` / CLI `--title` | 封面主标题 |
| 无显式大标题时的文档第一个 `#` | 封面主标题；同文标题不进正文 |
| frontmatter `subtitle` / AI `--subtitle` | 封面副标题 |
| 与封面大标题**不同**的正文 `#` | 按深度映射的正文标题（可为一级） |
| 排除封面重复后最浅的 `#` 深度（如 `##`） | 一级标题：01 / 02 编号 + 通栏分隔线 |
| 第二浅的 `#` 深度（如 `#####`） | 二级标题：强调色字 + 2px 浅色下划线 |
| 更深的标题 | 加粗正文段落 |
| 普通段落 | 可编辑、可按行跨页正文 |
| 单独一行 `---` / `***` / `___` | 手动强制换页（不出现在导出图；表格 `\| --- \|` 分隔行除外） |
| 整段加粗（18–75 字）或明确卡片标签开头 | 卡片块（标签：高亮/划重点/卡片/注意/结论/金句/关键/判断/提醒/重点/总结/小结/一句话/建议/提示/技巧/要点/避坑/误区/须知/干货） |
| `> 引用` / 飞书原生引用 | 斜体引用块 |
| fenced code（<code>```语言</code>） | 可编辑的 macOS 三色圆点代码块 |
| `-` / `*` / `+` | 无序序列 |
| `1.` / `1)` / `1、` | 有序序列 |
| `==文字==` | 有色字（行内强调色，可与加粗叠加） |
| `++文字++` | 下划线（行内半高亮 + 底线，可与加粗、有色字叠加） |
| Markdown pipe table / HTML `<table>` | 浅强调色表头、白底正文的表格块 |
| Markdown 图片 | 可编辑图片块 |
| 视频链接或附件 | 移除并记录警告 |

结构优先级必须高于关键词推断：引用里出现“金句”、列表里出现“卡片”时，仍保持引用和列表。

### 表格与输入细则

表格识别、手动分页、图片路径等 Markdown 约定见 `references/markdown-patterns.md`。列数过多时优先建议精简，不得静默删列；确需改卡片堆叠须先征得用户同意。

## 连续分页原则

- 普通正文组成字符连续流后按 3:4 画布高度分页；**手动分页**用单独一行 `---`（细则见 `references/markdown-patterns.md`）。
- 一级/二级标题、引用、卡片、代码块、图片和短表格是原子结构块：空间不足时整块移到下一页，不参与正文字符流，也不得从中间拆开。序列属于连续内容流，可以在完整序列项之间跨页；单个序列项的编号与正文保持绑定，不从中间拆开。只有超出一整页的长表格才按完整数据行跨页并重复表头（v0.8.69）。
- 块间距只存在于相邻块之间；页面最后一个块不保留无意义的底部段间距，图片、卡片等只要块本体能放下就应留在当前页。
- 连续序列在分页测量时保留相邻列表项上下文，不能把每一项都误算成序列末项；避免虚增项目间距后把本可放下的图片提前推到下一页（v0.8.56）。
- 总览编辑中，正文和封面副标题的回车、换行与双击选词由编辑区自行处理；Studio 不再提供单页编辑模式，点击另一页只会在总览中切换当前真实编辑画布。
- 正文采用飞书式段落交互：`Shift+Enter` 只在当前段内软换行；`Enter` 新建一个段落；连续回车会留下真实、可编辑且可删除的空段落。普通内容页开头用于跨页回填的空段仍可折叠，避免把下一页首字压到第二行；正文页完全为空时只保留一条真实的 58px 可编辑行。正文末尾空白处的点击光标与拖动插入线属于临时 gap/drop cursor，不得为了显示落点生成多行真实空段；自动保存或重新分页后必须恢复临时尾部光标，不能让它闪烁后坍缩为 0 高度。用户在该光标处按一次 Enter 时新增且仅新增一条真实空段（v0.9.1）。正文、列表及结构块开头均可用 Backspace/Delete 删除相邻空段落，删除后跨页回填。
- 分页只是普通正文与序列内容流的 3:4 视窗，不是独立编辑器。光标位于下一页续写首字前按 Backspace 时，必须删除上一页末尾的前一个字符并围绕光标重新分页；不同正文段落跨页相接时则按普通编辑器规则合并段落。引用、卡片、代码块等结构块只参与顺序与整块换页，不参与字符级回流（v0.8.69）。
- 在引用、卡片、标题、代码块等结构块开头按 Enter 时，插入真实手动空段。若新增空段把结构块完整推到下一页，结构块不得消失，光标和该空段应停留在上一页末尾；恢复光标时不得把承载光标的手动空段当成临时节点删除（v0.8.67）。
- 一级、二级标题分页时必须与紧随的正文或结构块保持在同一页；当前页剩余空间不足时标题随下一块整体移至下页，避免标题孤行挂在页尾（v0.8.59）。
- 无封面模式下正文按源顺序填入下半页，不跳过放不下的块。
- 编辑后 Studio 自动重排，无单独「重新分页」按钮。布局与导出约束见 `references/layout-spec.md`。

## Studio 能力

### 主题

- 默认背景 **纯白**、强调色 **知蓝**；知蓝保持原有标题、边框和下划线，只将卡片、表格表头等浅色承载面调整为更白的 `#F5FAFF`。图纸、背景、强调色、封面占位色可自由组合。

### 封面

- 封面支持三种形式并可在 Studio 右栏随时互切：**全封面**独占首张 3:4 页面，配图规格为 1080×1440；**半封面**上图、下标题与副标题，配图规格为 1080×720；**无封面**上标题与副标题、下半页续正文。
- 默认使用半封面，以兼容旧版输出。CLI 可通过 `--cover-mode full|half|none` 指定初始形式。
- 图纸纹理覆盖封面图片区和标题区；无封面时，标题区与下半页正文保持同一图纸观感。
- 封面占位色选择“跟强调”时使用浅强调色背景、标准强调色边框和深强调色提示文字。
- 标题按最多三行自动缩放。
- 副标题按中文字符权重最多 48 字、最多两行。
- 全封面与半封面显示封面占位色设置；无封面隐藏该设置。
- 封面图可点击替换、拖动裁剪和缩放。

### 文字与结构

- 工具栏使用 **H1 / H2 / B / A / U / ❝ / ▣ / `</>`** 与标准无序/有序列表图标，并根据当前光标或选中文字同步显示已应用样式；`</>` 用于 macOS 风格代码块。代码块正文为 32px / 1.55 行高；顶部三色圆点保留原尺寸、颜色与间距，并沿同一水平中心线排列。代码块与卡片使用同一套块级互切逻辑；普通正文必须先选中同一段内的有效文字，跨段或整页选区不得整体转换成代码块。编辑操作细则见 `docs/xhs-tool-intro.md`。
- 正文、序列、卡片和表格正文统一为 36px，引用正文保持 34px，卡片左上角标签为 26px；正文默认使用 720 加粗字重。选中文字点击 `B` 可切换为 700 不加粗，再点一次恢复 720；源 Markdown 的 `**加粗**` 保留语义且视觉字重为 720。Markdown 加粗不自动叠加下划线，下划线仅尊重原稿明确样式或 Studio 手动操作。转换前必须确认已安装字体栈第一优先级 `Noto Serif SC`，不得加入操作系统私有字体映射；一级标题、封面标题和大编号保留标题专用字重。
- 默认正文行高为 58px（36px 字号 + 22px 行间留白），段间距为 40px，正文四边安全边距统一为 72px。同一序列内部的项目间距为 20px，列表整体与上下结构仍使用 40px 段间距。一级标题下方引线保持原样，位于画布 174px；横线纸/方格纸以 58px 的第三行/格对齐引线，蓝图格以 43.5px 的第四格对齐引线。二级标题为 36px 正文规格的强调色下划线，正文结构块统一采用仅保留下间距的单向节奏：二级标题普通状态上间距为 0、后留 40px，页首前留 44px。因此一级标题、二级标题、引用、卡片、序列、图片等相接时不会叠加出双倍空白。
- 有序序号在独立编号栏内水平、垂直居中，与正文固定间隔 9px。列表项是样式按钮的最小操作单位：光标位于某一项时，只切换或取消该项，不得连带修改同页其他项。
- 序列交互遵循飞书块编辑：Enter 拆分当前项或续写下一项；在空序列项再次 Enter、或在空项开头 Backspace 时退出为普通空段；Shift+Enter 只在当前项内软换行。删光某项正文后，该项仍保留一个可编辑空正文和唯一序号/圆点，后续项自动顺延编号；只有用户在空项开头 Backspace 才删除该序列结构。非空序列项开头第一次 Backspace 去掉序号并退回正文，光标随立即重排保持在正文开头；第二次 Backspace 若前方是另一个序列项，则并入前一项正文而不新建序号；前方是普通正文时按正文跨页合并。正文位于引用、卡片或代码块之后时，在段首 Backspace 同样并入前一个块的正文位置，不整块选中删除（v0.8.71）。
- 有序/无序列表的标记只属于结构标记栏，绝不可写入正文内容。光标所在列表项点击同一列表按钮时，仅该项取消为正文；点击另一列表按钮时，仅该项切换类型，禁止重复嵌套或累加编号/圆点。连续序列跨过自动分页仍属于同一列表，编号由唯一连续流统一计算并跨页顺延；页面渲染不得从 1 重算。只有被普通正文、标题、引用、卡片、图片、表格、代码块或手动分页真正隔开时才重新起号。单个列表项为原子项，页尾放不下时必须整项移到下一页。
- 中文与英文统一继承同一套宋体字体栈，不再为英文单独指定 Times New Roman。
- 块样式按钮（一级标题 / 二级标题 / 引用块 / 卡片 / 序列）共用规则：选中文字后点击应用；光标在已有块内再点**同按钮**取消回正文；点**另一样式按钮**直接互切，不要求先取消。
- 卡片可切换竖边或细框，角标自动推断为“划重点 / 注意 / 金句 / 结论”。
- 竖边/细框只应用到当前选中的卡片，同页其他卡片保持各自样式。
- 卡片正文不重复显示角标文字；例如左上角已显示“金句”时，黑色正文会移除开头的“金句：”。
- 引用只转换选中内容，不把整段误变成引用。
- 悬浮在一级/二级标题、卡片、引用、序列或图片块上时，边界 `+` 每次增加一行空白；存在相邻空行时同时显示 `−`，每次减少一行，不要求用户精确放置文字光标。

### 正文编辑（连续流）

- **普通正文是字符连续流，不是独立文本框。** 所有正文段落先串成一条流，再按 3:4 画布实测高度分页；超长段落会按字符切开跨页，下一页从断点处一字接续，中间不插空行。
- **只有「切行」才断流：** Markdown 空行、Studio 里 Enter 新段、Shift+Enter 段内软换行、`---` 手动分页。标题、卡片、引用、序列、图片是结构块，保持完整，不拦腰切断。一个 Markdown 空行只负责分隔段落，不生成可见空白块；连续 N 个空行仅把多出的 N−1 个转换为手动空行。
- 普通正文边界**不显示** `+` / `−` 空行按钮；空行靠 Enter / Backspace / 删行，和平时写字一样。
- **Enter**：在普通正文里拆出新段落。**不会**删掉后面的标题、图片或其它块（v0.8.28 已修双触发吞字）。
- Enter 会先验证拆分前后正文逐字等价；有序/无序列表会保留类型、粗体和光标后的完整文字。浏览器只触发 `beforeinput` 时也使用目标 Range 处理。
- **Shift+Enter**：在同一段内软换行，不拆段。
- 删除空行后，Studio 会跨页 reflow，后面页文字自动往上接续填空。
- Enter、Backspace、序列退回正文以及块边界 `+ / −` 均按连续流触发全局 reflow；任何一页的首行和末行都不能成为删除或插入的硬边界。
- reflow 前后会核对正文、图片、手动空行和分页符的完整签名；若发现内容或顺序变化，会自动回滚并显示提示。
- reflow 重建页面时必须把折叠光标锚点带过分页，并在新页面恢复焦点与选区；不得因自动分页中断连续输入。
- 飞书导出在图片/标题/列表周围的空行**不会**自动转成空行块；正文↔正文、卡片→正文之间的单个空行也只作为分段符。只有连续多出的空行才保留为可增减的手动空行（v0.8.50）。
- Studio 只保留三页横向“总览编辑”，不再提供放大的单页编辑模式。点击页卡会在总览中激活该页的真实编辑画布；正文内容同时保存为唯一 `flowHtml` 连续流，`pages` 只作为当前 3:4 分页视图与导出结果。页卡显示 `当前页/总页数`。全局固定使用书刊宋体，不提供字体切换；转换前先安装字体栈第一优先级 `Noto Serif SC`，其余字体仅作为回退。正文默认统一为 720，因此 `B` 默认点亮；选中文字点一次切为 700 不加粗并熄灭，再点一次恢复 720（v0.9.0）。一级标题使用统一的两位数字固定占位与固定标题间隔，避免 `01 / 02 / 03` 宽度差异造成跳动（v0.8.63）。

### 图片

- 顶部工具栏提供“插入图片”按钮：先把光标放在正文中，再选择本地图片；图片写入该文档位置，若光标位于正文中间则拆分前后正文后插入。
- 支持直接粘贴剪贴板图片：按原始比例为横图、竖图和常规图片设置不同的初始图片框，再允许用户用右栏与控制点继续调整；不得一律按正文满宽误配横向截图。
- 单击选中，双击本地替换；不提供专门的“替换/删除”按钮。
- 裁剪填满、完整显示可切换。
- 鼠标拖动裁剪中心、滚轮缩放。
- 拖动蓝色控制点改变图片框宽高。
- 块级上移、下移、与前图/后图并排、拆回上下排列；悬浮区块时显示飞书式轻量六点手柄，也可按住 `Alt` 直接拖动标题、卡片、引用、代码块、表格、单个序列项或图片。拖动中只显示强调色插入横线，不生成透明区块克隆、气泡或整页遮罩；横线所在位置就是松手后的真实文档落点。落在普通正文中时按鼠标下的字符光标拆分前后正文；落在页尾空白时使用文档末尾的临时 drop cursor，松手只改变区块顺序，不生成占位空段。图片拖到目标页尾空白且图片框高于剩余空间时，自动收窄图片框高度至可用区域，避免顺序已经改变但图片仍被分页到下一页；不得用真实空段占位。若最小图片框仍放不下，则保持内容顺序并由全文分页（v0.8.81）。同页与跨页均适用，序列只移动当前项目。
- 无封面模式下，图片移动、并排与块级重排仍必须把封面下半页正文纳入同一条连续内容流，禁止只收集普通正文页造成内容遗漏（v0.8.72）。
- Delete / Backspace 可删除已选图片。

### 状态与输出

- localStorage 自动保存当前文章草稿；草稿键和源文件指纹包含 Skill 版本，重新生成或升级后不会被旧草稿覆盖。
- 结构编辑前保存安全检查点；localStorage 草稿正文缺失时会恢复检查点或源稿，并在 Studio 顶部明确提示。“保存编辑 HTML”只检查缺页、空流或断裂分片等结构损坏，用户主动删改的正文必须原样保留。
- “保存编辑 HTML”把当前状态嵌入新 HTML，重新打开仍保持编辑结果。
- “一键复原”确认后清除当前文章草稿并恢复初始内容、图片、主题和布局。
- PNG ZIP 导出必须与预览一致，每页尺寸相同，默认 1080 × 1440；先以 2 倍画布渲染，再高质量缩回标准尺寸以改善文字边缘。

## 输出结构

```text
<slug>-xhs/
├── xhs-studio.html
└── manifest.json
```

`manifest.json` 至少包含生成器、版本、标题、画布尺寸、布局参数、源文件路径、媒体警告和验证状态。

## 必做验证

代码或生成逻辑有改动时：

```bash
npm test
node --check scripts/convert.js
```

### 默认转换预览（每次）

1. 打开实际生成的 `xhs-studio.html`，不是只检查源码。
2. 逐页确认没有文字、图片和标题溢出边界。
3. 飞书引用显示为引用块，不被关键词误判为卡片。
4. 封面关闭再打开后，页数和内容顺序稳定。
5. 图片、卡片后能点击插入光标，且光标锚点不制造额外空行。
6. 一键复原能恢复标题、正文、图片、主题、字号和边距。
7. 至少抽查封面、引用页、图片页，确认 Studio 预览稳定。

### 导出验收（仅明确要求导出、验收或准备发布时）

1. 实际导出 PNG ZIP，检查文件数量和每张尺寸。
2. 至少抽查封面、引用页、图片页，确认导出与预览一致。
3. 用户只说“转换一下”“我看看”时，**不得**自动导出 ZIP；返回可编辑 `xhs-studio.html` 即可。

## 故障处理

- **找不到 Markdown**：要求用户提供完整导出包，或确认目录顶层存在 `.md`。
- **图片空白**：检查 Markdown 相对路径、URL 解码和附件是否仍在原目录。
- **视频未出现**：这是预期行为；向用户说明图文只放图片。
- **旧编辑状态覆盖新内容**：检查 `sourceFingerprint`，必要时使用“一键复原”。
- **页面溢出**：先检查结构块是否被误包进普通段落，再检查图片框高度和分页测量。
- **预览与导出不一致**：对比真实 PNG，检查 `html2canvas` 导出副本中的图片 transform、主题变量和下划线稳定化。

## 不在范围内

- 不负责把图文发布到小红书账号。
- 不负责视频转 GIF、视频上传或视频笔记发布。
- 不负责把飞书在线文档直接下载为 Markdown；本 Skill 从已导出的本地包开始。
- 不承诺所有浏览器渲染一致；开发与验收优先使用当前版 Chrome / Chromium。
